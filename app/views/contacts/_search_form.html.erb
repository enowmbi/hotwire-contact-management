<%= form_with(url: search_contacts_path, method: :post, data: { controller: "contacts", contacts_target: "form" }) do |form| %>
  <%= form.search_field :search_params, placeholder: "Enter contact to search", data: { action: "input->contacts#search" } %>
<% end %>
